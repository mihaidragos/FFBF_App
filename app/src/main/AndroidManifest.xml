<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ffbfapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FFBFApp">
        <activity android:name=".AssignCriticActivity"></activity>
        <activity android:name=".AdminPanelActivity" />
        <activity android:name=".AddStreetFoodActivity" />
        <activity android:name=".RestaurantsListActivity" />
        <activity android:name=".ChangePassActivity" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="Log In" />
        <activity
            android:name=".MainActivity"
            android:label="Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>